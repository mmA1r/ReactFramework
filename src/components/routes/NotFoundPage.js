import './NotFound.css'

function NotFound() {
    return (
        <div className="NotFoundPage" height={window.innerHeight} width={window.innerWidth}>
        
        </div>
    );
}

export default NotFound