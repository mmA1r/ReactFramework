import React from "react";
import './UIComponents/deleteFunction.css'

class DeleteFunction extends React.Component {
    render() {
        return(
            <button className = "deleteFunction"
            > Delete </button>
        );
    }
}

export default DeleteFunction;